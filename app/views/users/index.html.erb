<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%# @users.each do |user| %>
    <!-- <li> -->
      <%#= gravatar_for user, size: 52 %>
      <%#= link_to user.name, user %>
    <!-- </li> -->
  <%# end %>

  <%= render @users %>
  <!-- Rails infers @users is a list of User.objects
       and will render each one with _user.html.erb
   -->
</ul>

<%= will_paginate %>